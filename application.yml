spring: 
    profiles: 
        active:
           - dev
---
spring:
    profiles: dev
eureka:
  client:
    service-url:
      defaultZone: http://localhost:1111/eureka/
---
spring:
    profiles: test
eureka:
  client:
    service-url:
      defaultZone: http://localhost:1111/eureka/